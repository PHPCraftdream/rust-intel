# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local state
.claude/settings.local.json

# Project-local install target (when install.sh / install.ps1 are run without
# --user from inside the repo, files land here). The repo source of truth is
# rust-intel.md + commands/, not this dir.
/.claude/

# Future tooling artifacts
/target/
*.log

# Stray Rust build artifacts (e.g. from ad-hoc test compiles in the repo root)
*.rlib
*.rmeta
