# Review ledger

The five gap audits below were merged into v0.4.7. This ledger records the second-pass corrections so future audits do not treat historical review prose as current rule text.

| Review | v0.4.7 status | Second-pass action |
|---|---|---|
| crypto/secrets | Integrated | Corrected zeroization wording; moves/copies and reallocations are caveats, not a universal `memcpy` claim. Third pass restored the two worked examples the correction had dropped and removed editorial voice from the rule text. |
| FFI/unsafe | Integrated | Corrected the union rule: a read needs valid bits for the selected field type; a tag is required only for a tagged-union contract. Third pass restated it in BANNED form so the tagged-union case (the FFI-dominant one) is still a mechanically greppable finding, and moved the `transmute` "prefer a checked constructor" advice out of BANNED into REQUIRED. |
| deserialization/DoS | Integrated | Direct Serde struct visitors commonly reject duplicate fields; last-wins is a separate `Value`/map/custom-path policy. `serde_yaml` wording is version-aware. |
| concurrency/exhaustion | Integrated | No rule rollback; workflow now also audits admission-control artifacts outside `*.rs`. |
| supply-chain/build | Integrated | No rule rollback; workflow now includes lockfiles, toolchains, CI, scripts, and policy files. |
| crypto/secrets (2026-09 correction) | Corrected | §B12's JWT `aud`-validation grounding (`sources.md`, the `jsonwebtoken::Validation` entry) stated the mechanism backwards — reproduced against jsonwebtoken 9.3.1/11.0.0 and rewritten to the observed behavior (omitting `aud` is the dangerous accept, not carrying a foreign one). See `correctness-audit-2026-08.md` finding #1 / `fix-plan-2026-08.md` §2.1. |
| crypto/secrets (2026-09 correction) | Corrected | The same entry's `argon2::password_hash::SaltString::generate(&mut OsRng)` citation is ≤0.5-only; ≥0.6 moved to `generate_salt()`/`try_generate_salt()` under `rand_core ^0.10`. See `currency-audit-2026-08.md` S2 / `fix-plan-2026-08.md` §1.2. |
| deserialization/DoS (2026-09 correction) | Corrected | §B7's `sources.md` grounding overstated the `from_value`/`IgnoredAny`/`flatten` bypass of serde_json's 128-depth limit — reproduced (100k-deep unknown field via `IgnoredAny` returns `Ok`; only `from_value` on a programmatically-built `Value` overflows) and narrowed. See `correctness-audit-2026-08.md` finding #4 / `fix-plan-2026-08.md` §4.1. |
| commit `3bce0e1` message (2026-09 correction) | Corrected | The message claims a by-value `Self` method (`fn f(self)`) makes the whole trait dyn-incompatible, "confirmed with E0038"; on rustc 1.97 `trait T { fn f(self); }` still permits `&dyn T` — a by-value receiver only makes that one method non-dispatchable, it does not make the trait object-unsafe. The live `SKILL.md:8` wording does not repeat the error, so this is commit-message/provenance damage, not a remaining normative defect; recorded here rather than rewriting history. See `latest-commits-review-round-3-2026-09-02-1153.md` P3 (commit-message finding). |
| correctness-audit-2026-08 §E2 (2026-09 correction) | Corrected | The `inefficient_to_string` finding (lines 142-148) is severity-inflated: the `perf` → `pedantic` reclassification is right, but the audit's own text notes the prescribed Post-flight command already enables `pedantic`, so the prescribed pipeline does not miss the lint; P3 is proportionate, not P2. See `latest-commits-review-round-3-2026-09-02-1153.md` P3 (severity finding). |
| gap-audit-completeness-2026-08 (2026-09 correction) | Corrected | The report claims 63 candidates with an explicit candidate inventory and out-of-scope list, but numbers only 1-26 individually; 27-63 collapse into unnumbered "See Rejected and deferred" prose (lines 41, 261-277) that cannot be reconciled to 37 candidate IDs. Second instance of the arithmetic/provenance class already recorded in this ledger's "Historical-count erratum" below. See `latest-commits-review-round-3-2026-09-02-1153.md` P2 (candidate-inventory finding). |
| currency-audit-2026-08 (2026-09 correction) | Corrected | Grouped factual/severity errors at lines 96-118, 189-213, 235-242, 464-476: (a) S2 marks the `SaltString::generate` signature hypothesis "partially unverified" yet assigns HIGH; `password-hash` 0.6.1's verified `generate_salt()` API (behind `getrandom`) should have been distinguished from the unverified `rand_core`/`SysRng` path before severity was assigned; (b) S6 calls the reqwest TLS-method rename a blocker while its own text concedes the soft-deprecated aliases still work and the spec's trigger substring still matches — documentation staleness, not a detection failure; (c) the `lru` chronology dates the 0.18.2 patch 2026-08-03 as "after" the 2026-08-19 catalog fetch (an impossible order) and maps RUSTSEC-2026-0253 to double-panic-in-`Drop` rather than unsafe-state restoration during unwinding; (d) the Rust 1.88 let-chain item gives the year as 2026 instead of 2025 and calls the topic missing although `skill/drop-and-raii.md:42` already covers it (the §B4a let-chains bullet), contradicting `gap-audit-completeness-2026-08.md:250`; (e) `mismatched_lifetime_syntaxes` is an adjacent readability lint, not mechanical coverage of §B1's lifetime relations. See `latest-commits-review-round-3-2026-09-02-1153.md` P2 (currency-report findings). |
| latest-commits-review-round-3 (2026-09 correction) | Corrected | The round-3 report's commit-by-commit row for `d1f3df6` lists "broad redirect exemption" among proposed remedies needing correction, but no finding in that report substantiates the phrase, and the only redirect/SSRF remedy text — `gap-audit-completeness-2026-08.md` Gap 5's negative calibration — requires the opposite: `Policy::none()` or a `Policy::custom` that re-runs the destination check on every hop, plus resolving the hostname to the checked IP (`ClientBuilder::resolve`) so DNS rebinding cannot swap it. The live `skill/` modules carry no SSRF/redirect guidance at all (the guidance exists only as this report's non-integrated finding). Criticism withdrawn as unsupported. See `latest-commits-review-round-4-2026-09-02-1355.md` P2 (disposition-criticism finding). |
| latest-commits-review-round-3 (2026-09 correction) | Corrected | The report's verification section records an `npm pack --dry-run --json` integrity digest for commit `9a808f3` (`sha512-/pZI…`, 37 entries) without stating the Node/npm versions that produced it; re-running the same commit under Node 24.12.0/npm 11.13.0 yields a different digest (`sha512-nFpl…`), so an unpinned pack digest is not independently verifiable evidence. Policy going forward: any review citing a packaging/pack digest as evidence must pin and report the exact Node and npm versions used. See `latest-commits-review-round-4-2026-09-02-1355.md` P3 (digest-reproducibility finding). |
| latest-commits-review-round-4 (2026-09 correction) | Corrected | The round-4 report (`eb3b1d3`) corrects round-3's conclusions on several points — including the disposition-criticism and digest-reproducibility findings already recorded in the two round-3 correction rows above — but itself adds only report text with no ledger disposition row: the exact same failure round-4's own report attributes to round-3's `d095250`, and a violation of this ledger's rule that a corrected review's prose must not remain authoritative without a linking disposition. See `latest-commits-review-round-5-2026-09-02-1640.md` "Round 4 repeats the ledger defect it reports". |
| latest-commits-review-round-4 (2026-09 correction) | Corrected | The report's "Accepted P1 gaps that remain non-normative" table (`:126-137`) promotes four findings — `spawn_blocking` abort/timeout, secret-bearing `#[instrument]`, raw `BEGIN`/`COMMIT` through a pool, and positional-format compatibility — to an unconditional P1, when the source research rates each 🟡 by default and 🔴 only under a stated load-bearing condition (attacker-influenced duration, secret-bearing arguments, and a documented durability/wire-version guarantee, respectively). The live rules were independently confirmed already correctly conditionally-scoped (`skill/async.md`'s `spawn_blocking` and `#[instrument]` bullets already carry their 🔴-only-when qualifiers; `skill/drop-and-raii.md`'s pool-transaction bullet predates round-4), so this is report-text severity inflation, not a live-content defect. See `latest-commits-review-round-5-2026-09-02-1640.md` "Preserve conditional severities". |
| latest-commits-review-round-5 (2026-09 correction) | Corrected | Round-6 (`latest-commits-review-round-6-2026-09-02-1819.md`) characterizes round-5's four P1/conditional-P1 gaps (FFI destructor, poison recovery, `abort()`-as-graceful-shutdown, regex thread+channel hard-timeout) as "not fully closed by `d855da7`" — accurate as stated (that commit alone did not close them) but incomplete: they were closed by a separate, subsequent round-5 fixing pass applied directly to the working tree after the round-5 report was written and before round-6's review, which round-6's own stated scope ("revision-qualified committed blobs... pre-existing dirty worktree changes were excluded") could not observe. Not a defect in round-6's method — the same committed-vs-working-tree gap every round in this ledger has had — recorded here so the four gaps are not re-litigated as still-open. |
| this ledger, row above (2026-09 correction) | Corrected | The row above itself overstated closure: round-7 (`latest-commits-review-round-7-2026-09-02-2138.md`) found the FFI-destructor fix still let a caught panic payload's own drop reach the non-unwinding `extern "C"` boundary and abort (the payload was logged via a "type-erased Debug/Display path" that doesn't exist — `Any` provides neither), and the poison-recovery fix's `parking_lot::Mutex` exception was scoped to "no **cross-field** invariant", which wrongly implies a single-value invariant (a `Vec`, an enum state machine, a balance) is safe under it when it is not. Both are corrected in the commit that integrates round-7's findings; "closed" in the row above should be read as "closed as of that commit", not as of round-5's own fixing pass alone. |
| latest-commits-review-round-14 (2026-09 correction) | Corrected | The round-14 report's claim that link reference definitions are GFM block starts that break an open table is unsupported: cmark-gfm has no such block start (definitions are extracted from paragraph content at finalize time) and the table extension keeps the table open for any line that parses as a row with n_columns ≥ 1, so a pipe-less `[label]:` line directly under a table is a one-cell row — the exact thing the leading-pipe check exists to flag. dev/validate.mjs's block-start allowlist dropped the definition branch accordingly (and gained the indented-code-block start it was missing). See `docs/reviews/round-15-synthesis-2026-09-03-2146.md` Task 6. |
| latest-commits-review-round-14 (2026-09 correction) | Corrected | The round-14 report's finding that the module "still creates a false `unnameable_types` finding" mislocated the defect: the lint itself fires on alias-exposed types regardless of the module's wording (rust-lang/rust#120146, still open), so the remedy "include aliases in the public-surface definition" was right for the semver question but needed the lint caveat — the rule now states both that the alias is a real naming path and that `unnameable_types` does not yet model it, so a hit there is a known lint false positive, not evidence the type is outside the public surface. See `docs/reviews/round-15-synthesis-2026-09-03-2146.md` Task 2. |
| latest-commits-review-round-15-2026-09-03-1653-rush (2026-09 correction) | Corrected | The "phantom table" finding — that `a \| b \| c` / `\|---\|---\|` / `\|---\|---\|` / `x \| y` forms no GFM table, so the validator's flag on `x \| y` is wrong — is contradicted by cmark-gfm: try_opening_table_header takes the header row from the last line of a pending paragraph, which on the third line is the delimiter-shaped `\|---\|---\|` itself (hyphen-only header cells are legal), so `x \| y` is a real body row and the flag is correct. Repeated as the first half of latest-commits-review-round-15-2026-09-03-1653-final.md P3 #5. Withdrawn as a false alarm; only the validator's inline comment claiming "no table recognized" was wrong. See `docs/reviews/round-15-synthesis-2026-09-03-2146.md` False alarms §1. |
| `8d4db70` / round-22 correction (2026-09) | Reopened | The prior phantom-table withdrawal is preserved above as historical text; `8d4db70` reopens it as a separate disposition. cmark-gfm marks the containing paragraph `CMARK_NODE__TABLE_VISITED` after the first header/delimiter width mismatch, so a later delimiter is not retried and the sequence remains one paragraph. See cmark-gfm's [`table.c`](https://github.com/github/cmark-gfm/blob/master/extensions/table.c) (`TABLE_VISITED`, `try_opening_table_header`). |
| latest-commits-review-round-15-2026-09-03-1649 (2026-09 correction) | Corrected | The APPROVED verdict, its "new defect scan PASS — no new issues", and its "public type-alias nameability reconciled" closure are superseded: the reviewed commit introduced six `\\"` escape artifacts into two central trigger rows (one a shipped TOML recipe that cannot parse — round-15 synthesis Task 1) and an `unnameable_types` overcorrection (Task 2). Do not cite the 1649 approval as evidence. See `docs/reviews/round-15-synthesis-2026-09-03-2146.md`. |
| latest-commits-review-round-15-2026-09-03-1653-rush (2026-09 correction) | Corrected | Provenance erratum: the report records its review base as `2011a1b781f0e5bf6c46e04b8d05a5a656835c90`, which does not exist in this repository (`git cat-file -t` fails; the round-14 review commit is `2011a1bb4533a29d1961f9de317a85da59c323ba`), so its quoted diff range is not reproducible as written. Related: latest-commits-review-round-15-2026-09-03-1649.md states the range `d10427d..adee2e9`, which contains two commits (`2011a1b`, `adee2e9`) while its delta counts only one. See `docs/reviews/round-15-synthesis-2026-09-03-2146.md` Task 12. |
| round-15 synthesis Task 5 / latest-commits-review-round-15-2026-09-03-1653-final §3 (2026-09 correction) | Corrected | The 1653-final review's §3 proposed reducing the §F1 oracle to pairwise transpositions ("if a larger permutation is observationally invisible, at least two moved positions share the relevant observable signature, so swapping that pair is also invisible") and the synthesis's Task 5 propagated the reduction as sound ("the oracle itself is sound" — P3). The shared-signature premise is false under the mixed byte widths positional formats allow: `struct Record { a: [u8; 2], b: u8, c: u8 }` with `a = [1, 2], b = 1, c = 2` serializes as `01 02 01 02` — all three per-position signatures are distinct and every pairwise transposition is individually detectable under the swapped layout, yet the three-cycle to declaration order `b, c, a` decodes the calibration fixture's own bytes `01 02 01 02` back to the unchanged `b = 1, c = 2, a = [1, 2]` — which is exactly why a pairwise-only corpus built from this fixture stays green — while a different deployed record under the same schema, `a = [3, 4], b = 5, c = 6` (bytes `03 04 05 06`), decodes to the genuinely different `b = 3, c = 4, a = [5, 6]`, silently remapped with none of the pairwise checks or the calibration fixture ever revealing it. A pairwise reduction holds only under a stronger, stated premise (e.g. every reorderable position has equal byte width); the rule text now requires the exact-proposed-layout decode per actual change and retains the full permutation property otherwise. See `latest-commits-review-round-16-2026-09-03-2320.md` conditional-P1 finding 1. |
| latest-commits-review-round-18 (2026-09 correction) | Corrected | Both P3 findings closed: `dev/validate.mjs`'s `fenceCloser()` accepted any ECMAScript `\s` character (form feed, vertical tab, non-breaking space) after the closing-fence delimiter where cmark-gfm's closer scanner allows only ASCII space/tab, so a form-feed-suffixed line wrongly closed a fence and flagged a legitimate in-code `\"` as a leaked prose escape — the suffix class is narrowed to `[ \t]*` (all shipped markdown files are `\n`-ended; no CR in any scanned file); and the fixture suite lacked the tab-indented fake-closer control requested in round 17 — added alongside a form-feed-suffix false-closer control, with the diagnostic template literal now spelling `\\"` correctly. See `latest-commits-review-round-18-2026-09-04-0754.md` P3 findings 1-2. |
| latest-commits-review-rounds 20–21 (2026-09 closure) | Corrected | The two validator P3s are closed in the net architecture: the anchored contract's shared `projectFenceOpener` feeds the fence mask, and invalid backtick-info lines are body-width failures rather than table-boundary false negatives. The NBSP arbitrary-table delimiter case is moot because non-anchored tables are outside the anchored contract; surviving delimiter normalization still treats only the cmark-gfm ASCII whitespace class as table space. See `docs/reviews/latest-commits-review-round-20-2026-09-04-1206.md` findings 1–2 and `docs/reviews/latest-commits-review-round-21-2026-09-04-1228.md` carried P3 findings 1–2. |
| post-`a28b905` / post-`44eb16c` independent review cycles 1–8 (2026-09) | Superseded | Historical parser-emulation and scope-bounding work was replaced by the anchored repository contract. Git-history trace: `a28b905`, `44eb16c`, `8d4db70`, `80d8452`, `ee86c0d`, `014382a`, `a018845`, `3e8d433`, and `dbeddcd`; no current verification claim is carried by these intermediate records. |
| post-`44eb16c` independent review cycles 9–19 (2026-09) | Superseded | Historical hardening established code-state-only `MODULES`/`AUDIT_UNITS` checks, unique category ownership/body coverage, deep-freeze and policy pinning, complete workflow coverage, and the documented mutation boundary. Git-history trace: `0d6facb`, `11dc3df`, `d7bdf66`, `fa8ab55`, `68a2a6d`, `84be2bc`, `a60fa6d`, `9627995`, `f709981`, `044aca9`, and `848dbd4`; the current net state is recorded below and in the round-23 report. |
| post-cycle-19 independent review cycles 20–32 (2026-09) | Superseded | Historical parser and mutation-boundary refinements covered direct property/index and bracket forms, JavaScript line terminators, class and escaped-identifier boundaries, switch-label depth, optional chaining, and nullish operators. Git-history trace: `154f951`, `b29c1af`, `50ccd7a`, `3e56bc8`, `adbcf40`, `1714a40`, `a34ddfb`, `ec8e4a7`, `38129a9`, `2d0e5a8`, `fcb7bbb`, `5eb8691`, and `bd91e42`; intermediate control counts 238–347 are historical, the round-23/24 fixing revision carried 379 controls, the historical round-26 fixing state at `ae4df09`/`2890ea5` carried 384, the later fixing state carried 399, and the historical round-35 fixing state carried 409 before the reviewed 414-control head below. |
| Node runtime floor correction (2026-09) | Integrated | The supported runtime is Node.js `>=24.0.0` by support policy for the current LTS line: `package.json` records the package metadata, while both JavaScript installers and both validator entry points enforce the floor with hard startup guards. `fs.cpSync` has been available since Node 16.7.0 and explains only the former floor. Repository validators and the primary/publish workflows run on Node 24; CI also has a distinct exact `node-version: 24.0.0` floor job covering `npm run validate` and a real npm-installer smoke. The former Node 16.7.0 floor and compatibility job are historical and superseded. |
| latest-commits-review-round-23 and fixing pass (2026-09-06 disposition) | Integrated | The original unfiled `15afad0` "no P0-P3 issues" review claim remains uncheckable and is superseded by the committed round-23 review plus this fixing pass; the record and release-readiness issues round 23 found are closed here. It also records `0d6facb`'s duplicate category-owner/heading controls as covered by the later green CI run `34015308368` on descendant `15afad0`. Both CI jobs passed that reviewed revision; its source header and round-23 static recount established 375 controls (the runner itself reported two fixture cases, not the control count). Later totals 379, 384, 399, 409, and 414 belong to their respective fixing/reviewed revisions and are not current claims for this row. The net state is the anchored two-table contract, shared fence mask, bounded code-span and JavaScript mutation scanners, structurally parsed/deep-frozen workflow policy and hash coverage, and Node `>=24.0.0` guards; parser-only cycle probes remain retired as documented above. |
| latest-commits-review-round-24 and fixing pass (2026-09-06 disposition) | Integrated | Corrects round 23 Part 2 finding D: only the README banner carried `v0.6.0 (2026-08-19)`; the newest Status entry was `v0.5.0 (2026-08-08)`, so the earlier claim that the banner and Status list both carried `v0.6.0 (2026-08-19)` was inaccurate. The round-24 fixes correct the release checklist's ordering and Status/banner instructions, keep release-time control-count and changelog rewrites explicit, and record the review's ledger correction without asserting an unverified test result. |
| latest-commits-review-round-25 and rewritten fixing pass (2026-09-06 disposition) | Integrated | The round-25 report remains historical evidence for its old topology. The rewritten current history preserves its reviewed trees: `git diff e30bae3 ce57929` and `git diff efa62fb 5784fd2` are empty, so `e30bae3 == ce57929` and `efa62fb == 5784fd2` at tree level. The round-25 fixing pass is represented in current history by `c5aa58e` (release documentation and provenance), `ca5d24e` (full-SHA action pins plus Dependabot), and `ae4df09` (strict Unicode result classification and controls 380–384); the historical reviewed object `535eb60` maps to `ae4df09` at tree level (`git diff 535eb60 ae4df09` is empty), but is not a current-history commit. The round-26 review re-ran the relevant evidence: local `npm run validate` passed at Node 24.12.0, JavaScript syntax checks, `actionlint`, mirror and release-version checks, and package dry-run passed; all seven action refs re-resolved to their pinned SHAs. The old report's four-commit sequence is therefore superseded by these mapped trees and fixing commits; no CI result is claimed for the unpushed current head. |
| v0.5.1 historical release evidence (2026-09) | Noted | The npm registry records publication on 2026-08-14, which is the authoritative release date used by README and CHANGELOG. The repository currently has no `v0.5.1` Git tag; that absence is recorded explicitly rather than treated as ordinary tag evidence. |
| latest-commits-review-round-26 (2026-09-06 review) | Partially integrated | The report remains historical evidence for reviewed head `535eb60`, which maps tree-identically to current `ae4df09` (`git diff 535eb60 ae4df09` is empty; only `ae4df09` is an ancestor of the current history). The fixing pass is represented by `2890ea5`, `cb34859`, and `d969139`: `2890ea5` closes the Git-tag wording defect and updates release-facing counts; `cb34859` adds README/CHANGELOG copy-synchronization controls; and `d969139` synchronizes the then-current release-facing count from 384 to 388. Thus P3-1 (379-versus-384 release prose) and P3-4 (mutable-tag wording) are closed/superseded at that fixing state, while P3-2 (the false classifier-order explanation) and P3-3 (the missing rewritten-history/fixing disposition) remained open there. The later `7fcb71f` introduced control 389 and the 389 header, and `a5d833f` propagated 389 into README, CHANGELOG, and ledger prose; those are round-27 fixing commits and are disposed separately below. No CI result, version bump, tag, or push is claimed for the unpushed fixing history. See `docs/reviews/latest-commits-review-round-26-2026-09-06-1336.md`. |
| latest-commits-review-round-27 (2026-09-06 review) | Open | At reviewed head `d969139`, no P0–P2 remains, but four P3s block the repository's clean-review release gate: false classifier-order comments survived round 26; one ledger row still calls 384 current; rewritten-history mappings and the round-26 fixing disposition are incomplete; and release-facing count copies follow a hand-maintained header that is not mechanically proved equal to the actual numbered suite. See `docs/reviews/latest-commits-review-round-27-2026-09-06-1421.md`; no fixing-pass, CI, version-bump, tag, or push claim is made by this row. |
| latest-commits-review-round-27 fixing pass (2026-09-06 disposition) | Partially integrated | `7fcb71f` corrected the false Unicode classifier-order comments, introduced control 389 and the structural contiguous-label check; `1863bef` repaired the rewritten-history mapping (`535eb60 == ae4df09` at tree level) but did not by itself complete the round-27 fixing disposition; and `a5d833f` propagated the resulting 389 label total into README, CHANGELOG, and ledger prose. Round-27 P3-1 (classifier-order comments) and P3-2 (the stale 384 current-count claim) are closed. P3-3 (complete fixing provenance/disposition) is closed by this row. P3-4 (header-to-suite authority) remains open: the label inventory is not connected to executable control completion, so the executable-count gap must remain open until that connection and its counterfactuals are implemented. No CI result, version bump, tag, or push is claimed for this fixing history. |
| latest-commits-review-round-28 (2026-09-06 review) | Open | At reviewed head `a5d833f`, no P0–P2 remains, but three P3s still block the clean-review release gate: the new structural count treats comments as registrations without proving execution; the ledger misattributes the 389 transition and lacks a round-27 fixing disposition; and the release checklist still calls the already-present v0.6.0 Status entry missing. See `docs/reviews/latest-commits-review-round-28-2026-09-06-1504.md`; no fixing-pass, CI, version-bump, tag, or push claim is made by this row. |
| latest-commits-review-round-28 fixing pass (2026-09-06 disposition) | Partially integrated | `99f5310` closes round-28's checklist defect by requiring retention and verification of the existing `v0.6.0 (2026-08-19)` Status entry, and closes its provenance/attribution defect by restoring the round-26 count attribution and recording the round-27 fixing disposition. `ae39408` adds source-masked executable-control registry accounting and counterfactual scaffolding; `f276d09` binds accounting to assertion/outcome helper calls; and `4d12509` makes range accounting per-ID and adds omitted-middle/body-removal/add-next counterfactuals. Those three registry commits partially close round-28's executable-authority finding, but runtime cost amplification and semantic-body execution gaps remained open for round 29's follow-up corrections. Round-28's checklist and provenance items are closed; its registry item is only partially closed. No CI result, version bump, tag, or push is claimed for this unpushed fixing history. See `docs/reviews/latest-commits-review-round-28-2026-09-06-1504.md` and `docs/reviews/latest-commits-review-round-29-2026-09-06-1856.md`. |
| latest-commits-review-round-29 (2026-09-06 review) | Open | At reviewed head `4d12509`, no P0/P1 remains, but one P2 and two P3s block the clean-review release gate: recursive registry counterfactuals amplify integrated validation to 854.991 seconds (3.97x one reduced suite-equivalent); tautological assertion markers can still hide removal of a semantic control body; and the round-28 fixing pass has no ledger disposition. Round-28's checklist/provenance defects are closed by `99f5310`, while its executable-authority item is only partially integrated by `ae39408`, `f276d09`, and `4d12509`. See `docs/reviews/latest-commits-review-round-29-2026-09-06-1856.md`; no CI, version-bump, tag, or push claim is made for the unpushed reviewed head. |
| latest-commits-review-round-29 fixing pass (2026-09-06 disposition) | Partially integrated | `f085c36` closes round-29 P2-1 by replacing recursive near-full registry counterfactuals with bounded in-process helper cases. `b2f7f37` supplies the distinct round-28 fixing disposition, closing round-29 P3-2's missing-record defect while preserving round 29's historical Open row. `e5989b2` makes completed IDs explicit and rejects duplicate completed IDs, partially addressing round-29 P3-1; semantic-body execution remains open because the failure-baseline/default-true paths and unexercised control-389 counterfactuals identified by round 30 still remain. No CI result, version bump, tag, or push is claimed for this unpushed fixing history. See `docs/reviews/latest-commits-review-round-29-2026-09-06-1856.md` and `docs/reviews/latest-commits-review-round-30-2026-09-06-2014.md`. |
| latest-commits-review-round-30 (2026-09-06 review) | Open | At reviewed head `e5989b2`, round-29's recursive-runtime P2 is closed and the round-28 fixing disposition is present, but three P3s still block the clean-review release gate: twenty failure-baseline/default-true completion paths remain deletion-blind; control 389 does not exercise the retained-completion or foreign-registration counterfactuals it claims and accepts heavily polluted diagnostic sets; and the round-29 fixing pass has no ledger disposition. See `docs/reviews/latest-commits-review-round-30-2026-09-06-2014.md`; no CI, version-bump, tag, or push claim is made for the unpushed reviewed head. |
| latest-commits-review-round-30 fixing pass (2026-09-06 disposition) | Partially integrated | The historical round-30 Open row is retained. `d3d1c0e` closes round-30 P3-3 by recording the round-29 fixing pass. `6255730` removes the failure-baseline/default-outcome implementation defect and improves the bounded diagnostic cases, but round-30 P3-1 remains only partially closed because control 389 still lacks representative retained-completion source mutations. `ade4441` closes the polluted foreign-registration calibration, but round-30 P3-2 remains only partially closed for the same missing real source-mutation coverage. No CI result, version bump, tag, push, or publication claim is made for this unpushed fixing history. See `docs/reviews/latest-commits-review-round-30-2026-09-06-2014.md` and `docs/reviews/latest-commits-review-round-31-2026-09-06-2110.md`. |
| latest-commits-review-round-31 (2026-09-06 review) | Open | At reviewed head `ade4441`, no P0-P2 remains, but two P3s block the clean-review release gate: control 389 does not preserve the actual child/in-process completion predicates with bounded retained-completion source mutations and accepts literal-true completion; and the round-30 fixing pass has no disposition. The live former-baseline sites themselves now fail closed, exact bounded diagnostics cover duplicate-plus-omission/foreign registration/header drift, and the round-29 fixing disposition is present. See `docs/reviews/latest-commits-review-round-31-2026-09-06-2110.md`; no CI, version-bump, tag, push, or publication claim is made for the unpushed reviewed head. |
| latest-commits-review-round-31 fixing pass (2026-09-06 disposition) | Partially integrated | The historical round-31 Open row is retained. `7b36749` closes the real-source-mutation portion of P3-1: control 389 now mutates the actual control-1 and control-115 sources while retaining their labels, registrations, and completion expressions, and the bounded cases fail closed with the expected diagnostics. Its literal-true guard remains only partially integrated because parenthesized/ignored-extra-argument spellings and the duplicated lexer boundaries identified by round 32 can still bypass or misclassify the guard. `0c437fa` closes round-31 P3-2 by adding the distinct round-30 fixing disposition while preserving the historical round-30 Open row. No CI result, version bump, tag, push, publication, or release-readiness claim is made for this unpushed fixing history. See `docs/reviews/latest-commits-review-round-31-2026-09-06-2110.md` and `docs/reviews/latest-commits-review-round-32-2026-09-06-2149.md`. |
| latest-commits-review-round-32 (2026-09-06 review) | Open | At reviewed head `0c437fa`, no P0-P2 remains, but three P3s block the clean-review release gate: the literal-true completion guard accepts parenthesized and ignored-extra-argument forms; its duplicated JavaScript masker can hide a live completion behind postfix-division misclassification and treat regexp statement text as executable; and the round-31 fixing pass has no disposition. The actual control-1/control-115 retained-completion mutations and round-30 fixing disposition are present. See `docs/reviews/latest-commits-review-round-32-2026-09-06-2149.md`; no CI, version-bump, tag, push, or publication claim is made for the unpushed reviewed head. |
| latest-commits-review-round-32 fixing pass (2026-09-06 disposition) | Partially integrated | The historical round-32 Open row is retained. `412a8cb` closes round-32 P3-3 by recording the round-31 fixing disposition. `062d49c` closes the exact parenthesized-second-argument, ignored-extra-argument, postfix-division, `do`/`else` regexp, and duplicate-masker examples; the broader resource, regexp/division, and completion-call objectives remain open as recorded by round 33. No CI, version bump, tag, push, or publication claim is made for this local fixing history. See `docs/reviews/latest-commits-review-round-32-2026-09-06-2149.md` and `docs/reviews/latest-commits-review-round-33-2026-09-06-2304.md`. |
| latest-commits-review-round-33 (2026-09-06 review) | Open | At reviewed head `062d49c`, no P0-P1 remains, but four P2s and six P3s block the clean-review release gate: the shared lexer has unbounded-recursion and quadratic-input paths, still confuses live division with regexp data, the advertised POSIX clone scripts are not executable, npm does not sweep legacy layouts, literal-true call boundaries remain incomplete, install/version updates are not failure-atomic, Bash and PowerShell retain path-operand hazards, and round 32 has no fixing disposition. Round 32's exact parenthesized-second-argument, ignored-extra-argument, postfix-update, `do`/`else`, duplicate-masker, and missing-round-31-disposition cases are closed, but their broader lexical/completion goals remain partial. See `docs/reviews/latest-commits-review-round-33-2026-09-06-2304.md`; local validation passed in 238.901 seconds, but no CI, push, version bump, tag, or publication claim is made. |
| latest-commits-review-round-34 (2026-09-07 review) | Open | At reviewed head `18dc58a`, no P0-P1 remains, but three P2s and five P3s block the clean-review release gate. The round-33 fixing pass made the POSIX scripts executable, added npm legacy migration, removed recursive lexer traversal, staged installer replacements, added ordinary rollback, corrected literal PowerShell copying, normalized Bash targets, and recorded round 32. However, completion discovery remains quadratic; token context still hides `obj.if()` division and exposes regexp statements; PowerShell rollback can remove the working skill on a locked later path; interruption recovery is absent; the live literal-true gate filters nonliteral IDs; installer smokes are partial/vacuous; PowerShell uninstall is nontransactional; durable release-journal recovery is unproved; and the round-33 fixing/release record is absent. See `docs/reviews/latest-commits-review-round-34-2026-09-07-0000.md`; local validation, syntax, mirror, package, action-pin, manifest, and bounded calibration checks passed, but no current-head CI, push, version bump, tag, or publication claim is made. |
| latest-commits-review-round-33 fixing pass (2026-09-07 disposition) | Partially integrated | `8d71231` partially closes P2-1 by replacing recursive template scanning and the former line-prefix scan with bounded iterative lexical work, but completion-candidate discovery remains quadratic; it also partially closes P2-2/P3-1 while the surviving member-call, statement-block, and canonical-callee gaps are recorded by round 34. `230ef59` closes executable POSIX entry points and functionally closes npm legacy migration, while its rollback and regression coverage remain partial; `bf0112b` makes the three-manifest update exception-atomic, while durable interrupted recovery remains open; `18dc58a` closes ordinary PowerShell backup copying and adds a Windows rollback probe, but a locked later backup can still remove the active skill and interruption recovery is absent. No CI, version bump, tag, push, or publication claim is made for this fixing pass. See `docs/reviews/latest-commits-review-round-33-2026-09-06-2304.md` and `docs/reviews/latest-commits-review-round-34-2026-09-07-0000.md`. |
| latest-commits-review-round-35 (2026-09-07 review) | Open | At reviewed head `2266fbc`, no P0-P1 remains, but three P2s and five P3s still block the clean-review release gate. `6efe1c2` closes sampled completion/member/block cases but leaves an uncharged quadratic delimiter search, private-member division false negatives, and incomplete alias semantics. `4f9db28`/`2266fbc` close ordinary complete-inventory rollback and transactional PowerShell uninstall, but before-backup restart remains blocked, Bash can bind sparse backups to the wrong journal record, PowerShell can stage without a journal, and CI has no abrupt-restart oracle or oldest-Bash dash/rollback case. `3bd9bf5` adds 48-boundary process-exit release recovery, but nested plugin artifacts are outside its cleanup assertion and Windows power-loss ordering is not proved. Round-33 disposition and the 399 count are present; round-34 fixing disposition and current installer/Layout summary are absent. See `docs/reviews/latest-commits-review-round-35-2026-09-07-0056.md`; local full validation, actionlint, syntax, mirror, package, ordinary rollback, and release calibration passed, but no current-head CI, push, bump, tag, or publication claim is made. |

| latest-commits-review-round-34 fixing pass (2026-09-07 disposition) | Partially integrated | The historical round-34 Open row remains authoritative. `6efe1c2` closes the sampled completion/member/block cases, but the shared delimiter-budget gap, private-name role gap, and broader completion-alias contract remain open in round 35. `4f9db28` and `2266fbc` close caught-error complete-inventory rollback and transactional PowerShell uninstall; restart before backup, sparse Bash record binding, journal-less pre-live staging, and oldest-Bash interruption coverage remain open. `3bd9bf5` adds POSIX parent-directory barriers, recovery, and 48 process-exit release boundaries; the current calibration now recursively checks nested manifest artifacts and explicitly scopes Windows evidence to process interruption rather than sudden power loss. No version bump, tag, CI, push, or publication is claimed. See the round-34 report, its historical round-35 follow-up, and the current fixing commits. |

| latest-commits-review-round-35 fixing pass (2026-09-07 disposition) | Partially integrated | `992f734` closes round 35 P2-1 by rejecting mismatched delimiter nesting within the deterministic budget, P2-2 by preserving private-name roles, and P3-1 by enforcing the canonical completion-reference contract with controls 400–409. `64295f9` closes the implementation portion of P2-3 and P3-2 by journaling exact owned inventories before staging, recovering defined process-interruption boundaries across Node/Bash/PowerShell, binding sparse Bash records correctly, and adding restart plus Bash 3.2 coverage. `3fd68fa` closes P3-3's nested-artifact calibration gap, narrows P3-4 to POSIX parent-directory durability versus Windows process-interruption recovery (sudden power loss remains out of scope), and closes P3-5's release-record/Layout omissions. At that historical fixing state the suite was 409 controls, with 399 retained only as a qualified earlier count; the later 414-control review head is recorded separately below. No CI result, version bump, tag, push, or publication is claimed; a fresh independent review remains required. |

| latest-commits-review-round-36 (2026-09-07 review) | Open | At reviewed head `b476347`, no P0 remains, but one P1, two P2s, and four P3s block the clean-review release gate. `992f734` closes delimiter-budget and private-name cases but leaves indirect completion-reference bypasses. `64295f9` makes both Bash scripts exit at the first nonmatching abort hook under `set -e`; Node and PowerShell cannot recover after replacement rename, Node can publish an empty-record journal, and Bash still stores sparse backups by compact count. CI samples only backup-journal restart states, the release calibration accepts any nonzero abort status, and README/CHANGELOG/round-35 disposition overstate closure. See `docs/reviews/latest-commits-review-round-36-2026-09-07-0139.md`; full local validation passed in 239.433 seconds with the 409-control suite, but no current-head CI, push, bump, tag, or publication claim is made. |
| latest-commits-review-round-36 fixing pass (2026-09-07 disposition) | Partially integrated | The historical round-36 Open row remains authoritative. `671bd89` closes the exact indirect completion-reference cases represented by controls 410–414, but the broader static completion contract remains partial where function/class-expression division can be mistaken for a regexp. `035ceac` closes the implementation side of the first-journal, one-forward-replacement, and sparse-forward installer cases; `b6d3531` adds their restart and sparse-boundary coverage; and `5e6a19b` closes the ordinary Bash abort-hook reentrancy regression. `f435b0a` closes the release-calibration oracle by requiring exact child status `86` and adds the nonexistent-boundary normal-completion negative control. `933b31b` closes the negative inventory check for an unknown installer boundary, but the real rollback/recovery inventory and strict clean-operation restart oracle remain open. Thus the round-36 P1, its exact P2/P3 examples, and release-calibration status are closed or qualified above, while the broader recovery, matrix, lexer, and release-readiness objectives remain partial as found by round 37. No release, version bump, tag, CI, push, or publication claim is made here. |
| latest-commits-review-round-37 (2026-09-07 review) | Open | At reviewed head `933b31b`, round 36's ordinary Bash, full first-journal, one-forward-replacement, sparse-index, exact indirect-reference, and exact-86 release-calibration cases are fixed, but two P2s and three P3s still block the clean-review release gate. Installer rollback/recovery is not reentrant after a backup restore; function/class expressions can hide executable mutations or unconditional completions in a false regexp; the boundary matrix omits rollback/recovery and accepts the pre-operation snapshot after a successful restart; current Status/provenance remains stale; and the 414-control header still says 371 child plus 38 rather than 43 in-process controls. See `docs/reviews/latest-commits-review-round-37-2026-09-07-0254.md`; full validation passed in 235.931 seconds and bounded installer, release, syntax, action-pin, mirror, package, manifest, remote-tag, and npm checks were run, but no current-head CI, push, version bump, tag, or publication claim is made. |

## Historical-count erratum

The crypto review's prose count was inconsistent with its listed candidates. The integrated change set contains the five in-scope crypto findings plus two explicitly out-of-scope candidates; it is not a thirteen-item result. Use the rule ledger and changelog bullets as authoritative, not the old arithmetic sentence.

## Review quality gate

Every future gap review must include: (1) a candidate inventory, (2) positive and negative calibration examples, (3) a primary source per accepted rule, (4) an explicit out-of-scope list, and (5) a post-merge regression record in a **committed** artifact — the release's `CHANGELOG.md` entry, plus a row in the table above whenever the review corrects an earlier one. (`docs/checkpoints/` is deliberately untracked local session state and cannot carry a repo-level obligation.)
