# Review ledger

The five gap audits below were merged into v0.4.7. This ledger records the second-pass corrections so future audits do not treat historical review prose as current rule text.

| Review | v0.4.7 status | Second-pass action |
|---|---|---|
| crypto/secrets | Integrated | Corrected zeroization wording; moves/copies and reallocations are caveats, not a universal `memcpy` claim. Third pass restored the two worked examples the correction had dropped and removed editorial voice from the rule text. |
| FFI/unsafe | Integrated | Corrected the union rule: a read needs valid bits for the selected field type; a tag is required only for a tagged-union contract. Third pass restated it in BANNED form so the tagged-union case (the FFI-dominant one) is still a mechanically greppable finding, and moved the `transmute` "prefer a checked constructor" advice out of BANNED into REQUIRED. |
| deserialization/DoS | Integrated | Direct Serde struct visitors commonly reject duplicate fields; last-wins is a separate `Value`/map/custom-path policy. `serde_yaml` wording is version-aware. |
| concurrency/exhaustion | Integrated | No rule rollback; workflow now also audits admission-control artifacts outside `*.rs`. |
| supply-chain/build | Integrated | No rule rollback; workflow now includes lockfiles, toolchains, CI, scripts, and policy files. |
| crypto/secrets (2026-09 correction) | Corrected | §B12's JWT `aud`-validation grounding (`sources.md`, the `jsonwebtoken::Validation` entry) stated the mechanism backwards — reproduced against jsonwebtoken 9.3.1/11.0.0 and rewritten to the observed behavior (omitting `aud` is the dangerous accept, not carrying a foreign one). See `correctness-audit-2026-08.md` finding #1 / `fix-plan-2026-08.md` §2.1. |
| crypto/secrets (2026-09 correction) | Corrected | The same entry's `argon2::password_hash::SaltString::generate(&mut OsRng)` citation is ≤0.5-only; ≥0.6 moved to `generate_salt()`/`try_generate_salt()` under `rand_core ^0.10`. See `currency-audit-2026-08.md` S2 / `fix-plan-2026-08.md` §1.2. |
| deserialization/DoS (2026-09 correction) | Corrected | §B7's `sources.md` grounding overstated the `from_value`/`IgnoredAny`/`flatten` bypass of serde_json's 128-depth limit — reproduced (100k-deep unknown field via `IgnoredAny` returns `Ok`; only `from_value` on a programmatically-built `Value` overflows) and narrowed. See `correctness-audit-2026-08.md` finding #4 / `fix-plan-2026-08.md` §4.1. |
| commit `3bce0e1` message (2026-09 correction) | Corrected | The message claims a by-value `Self` method (`fn f(self)`) makes the whole trait dyn-incompatible, "confirmed with E0038"; on rustc 1.97 `trait T { fn f(self); }` still permits `&dyn T` — a by-value receiver only makes that one method non-dispatchable, it does not make the trait object-unsafe. The live `SKILL.md:8` wording does not repeat the error, so this is commit-message/provenance damage, not a remaining normative defect; recorded here rather than rewriting history. See `latest-commits-review-round-3-2026-09-02-1153.md` P3 (commit-message finding). |
| correctness-audit-2026-08 §E2 (2026-09 correction) | Corrected | The `inefficient_to_string` finding (lines 142-148) is severity-inflated: the `perf` → `pedantic` reclassification is right, but the audit's own text notes the prescribed Post-flight command already enables `pedantic`, so the prescribed pipeline does not miss the lint; P3 is proportionate, not P2. See `latest-commits-review-round-3-2026-09-02-1153.md` P3 (severity finding). |
| gap-audit-completeness-2026-08 (2026-09 correction) | Corrected | The report claims 63 candidates with an explicit candidate inventory and out-of-scope list, but numbers only 1-26 individually; 27-63 collapse into unnumbered "See Rejected and deferred" prose (lines 41, 261-277) that cannot be reconciled to 37 candidate IDs. Second instance of the arithmetic/provenance class already recorded in this ledger's "Historical-count erratum" below. See `latest-commits-review-round-3-2026-09-02-1153.md` P2 (candidate-inventory finding). |
| currency-audit-2026-08 (2026-09 correction) | Corrected | Grouped factual/severity errors at lines 96-118, 189-213, 235-242, 464-476: (a) S2 marks the `SaltString::generate` signature hypothesis "partially unverified" yet assigns HIGH; `password-hash` 0.6.1's verified `generate_salt()` API (behind `getrandom`) should have been distinguished from the unverified `rand_core`/`SysRng` path before severity was assigned; (b) S6 calls the reqwest TLS-method rename a blocker while its own text concedes the soft-deprecated aliases still work and the spec's trigger substring still matches — documentation staleness, not a detection failure; (c) the `lru` chronology dates the 0.18.2 patch 2026-08-03 as "after" the 2026-08-19 catalog fetch (an impossible order) and maps RUSTSEC-2026-0253 to double-panic-in-`Drop` rather than unsafe-state restoration during unwinding; (d) the Rust 1.88 let-chain item gives the year as 2026 instead of 2025 and calls the topic missing although `skill/drop-and-raii.md:42` already covers it (the §B4a let-chains bullet), contradicting `gap-audit-completeness-2026-08.md:250`; (e) `mismatched_lifetime_syntaxes` is an adjacent readability lint, not mechanical coverage of §B1's lifetime relations. See `latest-commits-review-round-3-2026-09-02-1153.md` P2 (currency-report findings). |
| latest-commits-review-round-3 (2026-09 correction) | Corrected | The round-3 report's commit-by-commit row for `d1f3df6` lists "broad redirect exemption" among proposed remedies needing correction, but no finding in that report substantiates the phrase, and the only redirect/SSRF remedy text — `gap-audit-completeness-2026-08.md` Gap 5's negative calibration — requires the opposite: `Policy::none()` or a `Policy::custom` that re-runs the destination check on every hop, plus resolving the hostname to the checked IP (`ClientBuilder::resolve`) so DNS rebinding cannot swap it. The live `skill/` modules carry no SSRF/redirect guidance at all (the guidance exists only as this report's non-integrated finding). Criticism withdrawn as unsupported. See `latest-commits-review-round-4-2026-09-02-1355.md` P2 (disposition-criticism finding). |
| latest-commits-review-round-3 (2026-09 correction) | Corrected | The report's verification section records an `npm pack --dry-run --json` integrity digest for commit `9a808f3` (`sha512-/pZI…`, 37 entries) without stating the Node/npm versions that produced it; re-running the same commit under Node 24.12.0/npm 11.13.0 yields a different digest (`sha512-nFpl…`), so an unpinned pack digest is not independently verifiable evidence. Policy going forward: any review citing a packaging/pack digest as evidence must pin and report the exact Node and npm versions used. See `latest-commits-review-round-4-2026-09-02-1355.md` P3 (digest-reproducibility finding). |
| latest-commits-review-round-4 (2026-09 correction) | Corrected | The round-4 report (`eb3b1d3`) corrects round-3's conclusions on several points — including the disposition-criticism and digest-reproducibility findings already recorded in the two round-3 correction rows above — but itself adds only report text with no ledger disposition row: the exact same failure round-4's own report attributes to round-3's `d095250`, and a violation of this ledger's rule that a corrected review's prose must not remain authoritative without a linking disposition. See `latest-commits-review-round-5-2026-09-02-1640.md` "Round 4 repeats the ledger defect it reports". |
| latest-commits-review-round-4 (2026-09 correction) | Corrected | The report's "Accepted P1 gaps that remain non-normative" table (`:126-137`) promotes four findings — `spawn_blocking` abort/timeout, secret-bearing `#[instrument]`, raw `BEGIN`/`COMMIT` through a pool, and positional-format compatibility — to an unconditional P1, when the source research rates each 🟡 by default and 🔴 only under a stated load-bearing condition (attacker-influenced duration, secret-bearing arguments, and a documented durability/wire-version guarantee, respectively). The live rules were independently confirmed already correctly conditionally-scoped (`skill/async.md`'s `spawn_blocking` and `#[instrument]` bullets already carry their 🔴-only-when qualifiers; `skill/drop-and-raii.md`'s pool-transaction bullet predates round-4), so this is report-text severity inflation, not a live-content defect. See `latest-commits-review-round-5-2026-09-02-1640.md` "Preserve conditional severities". |
| latest-commits-review-round-5 (2026-09 correction) | Corrected | Round-6 (`latest-commits-review-round-6-2026-09-02-1819.md`) characterizes round-5's four P1/conditional-P1 gaps (FFI destructor, poison recovery, `abort()`-as-graceful-shutdown, regex thread+channel hard-timeout) as "not fully closed by `d855da7`" — accurate as stated (that commit alone did not close them) but incomplete: they were closed by a separate, subsequent round-5 fixing pass applied directly to the working tree after the round-5 report was written and before round-6's review, which round-6's own stated scope ("revision-qualified committed blobs... pre-existing dirty worktree changes were excluded") could not observe. Not a defect in round-6's method — the same committed-vs-working-tree gap every round in this ledger has had — recorded here so the four gaps are not re-litigated as still-open. |
| this ledger, row above (2026-09 correction) | Corrected | The row above itself overstated closure: round-7 (`latest-commits-review-round-7-2026-09-02-2138.md`) found the FFI-destructor fix still let a caught panic payload's own drop reach the non-unwinding `extern "C"` boundary and abort (the payload was logged via a "type-erased Debug/Display path" that doesn't exist — `Any` provides neither), and the poison-recovery fix's `parking_lot::Mutex` exception was scoped to "no **cross-field** invariant", which wrongly implies a single-value invariant (a `Vec`, an enum state machine, a balance) is safe under it when it is not. Both are corrected in the commit that integrates round-7's findings; "closed" in the row above should be read as "closed as of that commit", not as of round-5's own fixing pass alone. |
| latest-commits-review-round-14 (2026-09 correction) | Corrected | The round-14 report's claim that link reference definitions are GFM block starts that break an open table is unsupported: cmark-gfm has no such block start (definitions are extracted from paragraph content at finalize time) and the table extension keeps the table open for any line that parses as a row with n_columns ≥ 1, so a pipe-less `[label]:` line directly under a table is a one-cell row — the exact thing the leading-pipe check exists to flag. dev/validate.mjs's block-start allowlist dropped the definition branch accordingly (and gained the indented-code-block start it was missing). See `docs/reviews/round-15-synthesis-2026-09-03-2146.md` Task 6. |
| latest-commits-review-round-14 (2026-09 correction) | Corrected | The round-14 report's finding that the module "still creates a false `unnameable_types` finding" mislocated the defect: the lint itself fires on alias-exposed types regardless of the module's wording (rust-lang/rust#120146, still open), so the remedy "include aliases in the public-surface definition" was right for the semver question but needed the lint caveat — the rule now states both that the alias is a real naming path and that `unnameable_types` does not yet model it, so a hit there is a known lint false positive, not evidence the type is outside the public surface. See `docs/reviews/round-15-synthesis-2026-09-03-2146.md` Task 2. |
| latest-commits-review-round-15-2026-09-03-1653-rush (2026-09 correction) | Corrected | The "phantom table" finding — that `a \| b \| c` / `\|---\|---\|` / `\|---\|---\|` / `x \| y` forms no GFM table, so the validator's flag on `x \| y` is wrong — is contradicted by cmark-gfm: try_opening_table_header takes the header row from the last line of a pending paragraph, which on the third line is the delimiter-shaped `\|---\|---\|` itself (hyphen-only header cells are legal), so `x \| y` is a real body row and the flag is correct. Repeated as the first half of latest-commits-review-round-15-2026-09-03-1653-final.md P3 #5. Withdrawn as a false alarm; only the validator's inline comment claiming "no table recognized" was wrong. See `docs/reviews/round-15-synthesis-2026-09-03-2146.md` False alarms §1. |
| `8d4db70` / round-22 correction (2026-09) | Reopened | The prior phantom-table withdrawal is preserved above as historical text; `8d4db70` reopens it as a separate disposition. cmark-gfm marks the containing paragraph `CMARK_NODE__TABLE_VISITED` after the first header/delimiter width mismatch, so a later delimiter is not retried and the sequence remains one paragraph. See cmark-gfm's [`table.c`](https://github.com/github/cmark-gfm/blob/master/extensions/table.c) (`TABLE_VISITED`, `try_opening_table_header`). |
| latest-commits-review-round-15-2026-09-03-1649 (2026-09 correction) | Corrected | The APPROVED verdict, its "new defect scan PASS — no new issues", and its "public type-alias nameability reconciled" closure are superseded: the reviewed commit introduced six `\\"` escape artifacts into two central trigger rows (one a shipped TOML recipe that cannot parse — round-15 synthesis Task 1) and an `unnameable_types` overcorrection (Task 2). Do not cite the 1649 approval as evidence. See `docs/reviews/round-15-synthesis-2026-09-03-2146.md`. |
| latest-commits-review-round-15-2026-09-03-1653-rush (2026-09 correction) | Corrected | Provenance erratum: the report records its review base as `2011a1b781f0e5bf6c46e04b8d05a5a656835c90`, which does not exist in this repository (`git cat-file -t` fails; the round-14 review commit is `2011a1bb4533a29d1961f9de317a85da59c323ba`), so its quoted diff range is not reproducible as written. Related: latest-commits-review-round-15-2026-09-03-1649.md states the range `d10427d..adee2e9`, which contains two commits (`2011a1b`, `adee2e9`) while its delta counts only one. See `docs/reviews/round-15-synthesis-2026-09-03-2146.md` Task 12. |
| round-15 synthesis Task 5 / latest-commits-review-round-15-2026-09-03-1653-final §3 (2026-09 correction) | Corrected | The 1653-final review's §3 proposed reducing the §F1 oracle to pairwise transpositions ("if a larger permutation is observationally invisible, at least two moved positions share the relevant observable signature, so swapping that pair is also invisible") and the synthesis's Task 5 propagated the reduction as sound ("the oracle itself is sound" — P3). The shared-signature premise is false under the mixed byte widths positional formats allow: `struct Record { a: [u8; 2], b: u8, c: u8 }` with `a = [1, 2], b = 1, c = 2` serializes as `01 02 01 02` — all three per-position signatures are distinct and every pairwise transposition is individually detectable under the swapped layout, yet the three-cycle to declaration order `b, c, a` decodes the calibration fixture's own bytes `01 02 01 02` back to the unchanged `b = 1, c = 2, a = [1, 2]` — which is exactly why a pairwise-only corpus built from this fixture stays green — while a different deployed record under the same schema, `a = [3, 4], b = 5, c = 6` (bytes `03 04 05 06`), decodes to the genuinely different `b = 3, c = 4, a = [5, 6]`, silently remapped with none of the pairwise checks or the calibration fixture ever revealing it. A pairwise reduction holds only under a stronger, stated premise (e.g. every reorderable position has equal byte width); the rule text now requires the exact-proposed-layout decode per actual change and retains the full permutation property otherwise. See `latest-commits-review-round-16-2026-09-03-2320.md` conditional-P1 finding 1. |
| latest-commits-review-round-18 (2026-09 correction) | Corrected | Both P3 findings closed: `dev/validate.mjs`'s `fenceCloser()` accepted any ECMAScript `\s` character (form feed, vertical tab, non-breaking space) after the closing-fence delimiter where cmark-gfm's closer scanner allows only ASCII space/tab, so a form-feed-suffixed line wrongly closed a fence and flagged a legitimate in-code `\"` as a leaked prose escape — the suffix class is narrowed to `[ \t]*` (all shipped markdown files are `\n`-ended; no CR in any scanned file); and the fixture suite lacked the tab-indented fake-closer control requested in round 17 — added alongside a form-feed-suffix false-closer control, with the diagnostic template literal now spelling `\\"` correctly. See `latest-commits-review-round-18-2026-09-04-0754.md` P3 findings 1-2. |
| latest-commits-review-rounds 20–21 (2026-09 closure) | Corrected | The two validator P3s are closed in the net architecture: the anchored contract's shared `projectFenceOpener` feeds the fence mask, and invalid backtick-info lines are body-width failures rather than table-boundary false negatives. The NBSP arbitrary-table delimiter case is moot because non-anchored tables are outside the anchored contract; surviving delimiter normalization still treats only the cmark-gfm ASCII whitespace class as table space. See `docs/reviews/latest-commits-review-round-20-2026-09-04-1206.md` findings 1–2 and `docs/reviews/latest-commits-review-round-21-2026-09-04-1228.md` carried P3 findings 1–2. |
| post-`a28b905` independent Sol-high review cycle 1 (2026-09 closure) | Superseded | The preliminary scope covered fenced-content suppression; CRLF/lone-CR parsing; optional 0–3-space leading pipes; paragraph-aware interruption; escaped-pipe handling; duplicate signatures scoped to confirmed tables; and NBSP rollback controls. Its odd/even escaped-pipe behavior is a repository raw-pipe convention, not cmark/GFM behavior. `014382a` superseded the parser-emulation portions with the anchored contract. No separate review report was created; this records the supersession without asserting test results or a new commit. |
| post-`44eb16c` independent Sol-high review cycle 2 (2026-09 closure) | Superseded | The preliminary closure scope covered one-column tables; paragraph visited after width mismatch; HTML start/continuation handling; body-only duplicate signatures; CommonMark code-span extraction; and nested blockquote fences. Cycle 3's P3 corrections supersede its claim of closure; no separate review report was created, and this row makes no assertion about test results or a new commit. |
| post-`44eb16c` independent Sol-high review cycle 3 (2026-09 closure) | Superseded | The preliminary closure scope covered fully pipe-less/mixed one-column headers; setext boundary after `TABLE_VISITED`; exact CommonMark type-1 terminators; list-contained HTML/fences; inline HTML/autolink precedence for code signatures; injective signature keys; and strengthened oracles. Cycle 4's P3 corrections supersede its claim of closure; no separate review report was created, and this row makes no assertion about test results or a new commit. |
| post-`44eb16c` independent Sol-high review cycle 4 (2026-09 closure) | Superseded | The preliminary closure scope covered accurate list-container lifecycle (siblings, residual markers, blanks, interruption, and padding); 0–3-space HTML starts; inline link/image destinations and titles; exact inline HTML priority; autolink bounds; and strengthened table-leak/parity oracles. Cycle 5's P3 corrections supersede its claim of closure; no separate review report was created, and this row makes no assertion about test results or a new commit. |
| post-`44eb16c` independent Sol-high review cycle 5 (2026-09 closure) | Superseded | The preliminary closure scope described an architectural correction to validator scope: the validator intentionally validates the two anchored top-level trigger tables and a documented repository Markdown subset; it does not claim to parse arbitrary GFM containers or inline links. Unsupported raw HTML, same-line container-prefixed fences, and complex inline markup in code-pattern cells fail explicitly; the bounded linear code-span duplicate scan remains. Cycle 6's contract correction supersedes this closure; no separate review report was created, and this row makes no assertion about test results or a new commit. |
| post-`44eb16c` independent Sol-high review cycle 6 (2026-09 closure) | Superseded | The preliminary closure scope covered exactly two global top-level trigger-table anchors, raw column-1 pipe/width/body rules, a bounded plain-text + inline-code subset, standalone project fences, and explicit rejection of unsupported container/angle-leading syntax. Cycle 7's contract precision supersedes its wording; no separate review report was created, and this row makes no assertion about test results or a new commit. |
| post-`44eb16c` independent Sol-high review cycle 7 (2026-09 closure) | Superseded | Maintainer-facing validator contract: exactly two global top-level trigger-table anchors with canonical end/scaffold boundaries; raw column-1 pipes, declared widths, and nonempty bodies; continuation through the `Category map` parity check; duplicate keys are the sorted set of inline-code tokens and prose-only rows are not deduplicated; ordinary emphasis/strong/strikethrough is allowed, while bracketed link/image/reference-like syntax, angle-leading raw-HTML/autolink-like syntax, and the broad repository-style URI/email-like ban outside inline code are rejected; standalone project fences allow 0–3 leading spaces; and the validator is a documented repository subset, not an exact GFM parser. Cycle 8's contract precision supersedes this wording; no separate review report was created, and this row makes no assertion about test results or a new commit. |
| post-`44eb16c` independent Sol-high review cycle 8 (2026-09 closure) | Superseded | The preliminary closure scope covered fence-aware category parity; executable workflow parity; bounded child processes; one fixture aggregation path; the supported Node 16.7.0 floor; and the maintainer-facing contract that any raw `<` outside inline code in a code-pattern row's first cell is unsupported. Cycle 9's P3 corrections supersede its claim of closure; no separate review report was created, and this row makes no assertion about test results or a new commit. |
| post-`44eb16c` independent Sol-high review cycle 9 (2026-09 closure) | Corrected | Closure scope: code-state-only `MODULES` assignment; unique category owners and bodies; raw `<` outside inline-code spans; and the corrected linear stress oracle. No separate review report was created; this row records the closure scope without asserting test results or a new commit. |
| latest-commits-review-round-22 / post-`44eb16c` independent Sol-high review cycle 10 (2026-09 disposition) | Superseded | The round-22 disposition and its restored regression controls are superseded by cycle 11's integration record below. The anchored contract still retires parser-only one-column, HTML-block, list-container, and `TABLE_VISITED` probes as moot; the carried fence, NBSP, CRLF/lone-CR, invalid-info, and raw-column-1 controls remain required. |
| post-`11dc3df` independent Sol-high review cycle 11 (2026-09 disposition) | Superseded | This integration record is superseded by cycle 12. Its scope remains historical: complete-array `AUDIT_UNITS` parsing, fence-aware rule-text oracles, exact nested-fixture opt-out handling, corrected NBSP/tab-indentation diagnostics, and the watchdog policy. It makes no current verification or control-count claim. |
| post-cycle-11 / latest-commits-review-round-22 follow-up, independent Sol-high cycle 12 (2026-09 disposition) | Superseded | This integration record is superseded by cycle 13. Its scope remains historical: immutable, policy-pinned `AUDIT_UNITS` parsing from the complete workflow array, runtime module matching, the complete fixture/oracle set, and the CI installer-smoke configuration. No test, CI, fixture-count, or active-install parity claim is carried forward from that record. |
| post-cycle-12 independent Sol-high cycle 13 (2026-09 closure) | Superseded | Historical scope: runtime `MODULES` and `AUDIT_UNITS` are deep-frozen with pinned policies; module identity mismatches are checked structurally; category cells are strict; rule-text oracles are structurally scoped; and NBSP handling/diagnostics are corrected. Historical verification remains recorded: `npm run validate` passed (the main validator printed 12 skill Markdown files and invoked the child fixture runner); direct `node dev/validate-fixtures.mjs` passed (2 cases; its source header declares 144 controls); separate `node --check` passed for the validator, fixture runner, `bin/install.js`, and both canonical/mirror workflow files; `node dev/sync-mirror.mjs --check` passed for 13 files; and `skill-creator` quick validation passed for the canonical and mirror skills under `PYTHONUTF8=1`. The initial locale-default quick-validation attempt failed on cp1252 decoding, an environment/tool issue, and was not treated as a project defect. CI remained pending, so no Node 16.7.0 CI result was claimed. The active Codex installation was temporarily stale after the workflow change and was scheduled for reinstall after the final review; no current parity claim is carried forward. Cycle 14 supersedes this closure record. |
| post-cycle-13 independent Sol-high cycle 14 (2026-09 closure) | Superseded | Historical scope: immutable `const` deep-freeze helpers; structurally pinned live missing-input iteration and the five-gate `orchestrationComplete` formula; and calibrated freeze, NBSP, category, and catalog oracles. Historical verification remains recorded: direct `node dev/validate-fixtures.mjs` passed (2 cases; its source header declares 155 controls); `npm run validate` passed and the main validator printed 12 skill Markdown files; separate `node --check` passed for the validator, fixture runner, and canonical/mirror workflow files; `node dev/sync-mirror.mjs --check` passed for 13 files; and `skill-creator` `quick_validate` passed for the canonical and mirror skills under `PYTHONUTF8=1`. CI remained pending, so no Node 16.7.0 CI result was claimed. The active Codex installation remained stale until the final reinstall; no current install-parity claim is carried forward. Cycle 15 supersedes this closure record. |
| post-cycle-14 independent Sol-high cycle 15 (2026-09 closure) | Superseded | Historical closure scope: a fully pinned artifact/docs/final missing-input loop; declaration-anchored structural regex checks; correct trailing-elision handling; transitive-alias mutation rejection; and unique in-scope catalog/list oracles. The cycle-15 source header and its recorded run said 165 hand-written controls; cycle 16's recount corrects the effective total to 166. The 165-control verification remains historical and is not silently rewritten. Cycle 16 supersedes this closure record. |
| post-cycle-15 independent Sol-high cycle 16 (2026-09 closure) | Superseded | Historical scope: exact semantic pinning of the live missing-input block; the semicolon-terminated five-gate `orchestrationComplete` formula; template-substitution mutation scanning; calibrated reference-alias tracking; unique `Operating mode`/item oracles; corrected fixture-control numbering (181 effective hand-written controls); and the UTF-16 offset fix (`split('')`, not `Array.from`) that was required after the baseline failure. Verification remains historical exactly: direct `node dev/validate-fixtures.mjs` passed (2 cases; its source header declares 181 controls); `npm run validate` passed and the main validator printed 12 skill Markdown files; separate `node --check` passed for the validator and fixture runner; the prior canonical/mirror workflow syntax checks and `node dev/sync-mirror.mjs --check` remain passed. CI remains pending, so no Node 16.7.0 CI result is claimed. The active Codex installation remains pending final reinstall; no current install-parity claim is made. Cycle 15's 165-control run and cycle 16's intermediate 166-control recount remain historical evidence. Cycle 17 supersedes this closure record. |
| post-cycle-16 independent Sol-high cycle 17 (2026-09 follow-up) | Superseded | Scope: the hash-pinned complete-coverage producer block; helper-before-call ordering; calibrated parenthesized/destructured alias handling; and corrected section, disjunction, and helper oracles. Cycle 17's 192-control closure record is superseded by cycle 18's narrower declarative workflow contract. Its recorded verification is historical and is not silently carried forward. |
| post-cycle-17 independent Sol-high cycle 18 (2026-09 follow-up) | Superseded | Historical scope: `MODULES` and `AUDIT_UNITS` remained direct, deep-frozen declarative initializers; inline consumption, direct `.length` mutation coverage, and calibrated alias controls were covered. Its allowance for bound `.map()`/`.filter()` results was unsound because descendants may retain references to frozen records. The 217-control verification remains historical and is not silently rewritten. Cycle 19 supersedes this contract. |
| post-cycle-18 independent Sol-high cycle 19 (2026-09 follow-up) | Integrated | Scope: preserve direct, deep-frozen `MODULES`/`AUDIT_UNITS` initializers; allow inline consumption and primitive `.length` bindings; reject direct RHS-leading aliases, destructuring, and bound `.map()`/`.filter()` results because descendants may retain frozen-record references; rewrite workflow consumers to explicit primitive-projection loops; and pin fixed `.length` oracles with corrected comment ranges. Verification recorded by `848dbd4`: `npm run validate` passed; the direct fixture runner passed its 2 cases (217 sequential controls declared); separate Node syntax checks passed; and `dev/sync-mirror.mjs --check` passed for 13 files. Node 16.7 CI and active Codex-install parity remain pending. Cycle 18's 217-control evidence remains historical. |
| post-cycle-19 independent Sol-high cycle 20 (2026-09 follow-up) | Superseded | Historical scope: pin the exact declarator RHS for the live `MODULES`/`AUDIT_UNITS` initializers; permit primitive `.length` bindings in dot/bracket form, with or without full parentheses; reject the complete update/compound-assignment operator set; and isolate every binding-site oracle so unrelated declarations cannot satisfy it. Verification completed locally: `npm run validate` passed and the main validator printed 12 skill Markdown files; direct `node dev/validate-fixtures.mjs` passed its 2 cases (the source header declares 238 sequential controls); and separate `node --check` checks for the validator and fixture runner passed. Cycle 19's integrated fixed `.length` oracle range comments remain historical evidence. Node 16.7 CI and active Codex-install parity remain pending; no current claim is made for either. Cycle 21 supersedes this contract. |
| post-cycle-20 independent Sol-high review cycle 21 (2026-09 follow-up) | Superseded | Historical scope: narrow the workflow mutation contract. The validator statically rejects direct/bound reference aliases, direct property/index mutations, bracket mutators, and parenthesized, update, or compound-assignment forms. Indirect record provenance through `at`/`find`, `for...of` variables, and callbacks is not statically modeled; `deepFreeze` remains the runtime fail-fast backstop. This contract does not claim that all mutations are statically rejected. Verification remains historical exactly: `npm run validate` passed and the main validator printed 12 skill Markdown files; direct `node dev/validate-fixtures.mjs` passed its 2 cases (the source header declares 245 sequential controls); and separate `node --check` checks for the validator and fixture runner passed. Cycle 22 supersedes this closure record; CI and active Codex-install parity remain pending. |
| post-cycle-21 independent Sol-high review cycle 22 (2026-09 follow-up) | Superseded | Its closure scope was a comment-tolerant parser for direct property/index and bracket forms, plus control-header-aware matching for grouped, update, and compound-assignment mutations. The cycle-21 245-control result and cycle-22's 255-control verification remain historical evidence and are not silently rewritten. Cycle 23 supersedes this record; no current test, fixture-count, CI, or active Codex-install parity claim is carried forward. |
| post-cycle-22 independent Sol-high review cycle 23 (2026-09 follow-up) | Superseded | Historical scope: JavaScript `LineTerminator`-aware update parsing; matching real control-keyword tokens while rejecting property-method lookalikes; and causal `else`/`do` controls that prove the parser reaches the relevant mutation path. Historical verification remains exactly as recorded: `npm run validate` passed and the main validator printed 12 skill Markdown files; direct `node dev/validate-fixtures.mjs` passed its 2 cases (the source header declared 261 sequential controls); and separate `node --check` checks passed for the validator and fixture runner. CI and active Codex-install parity were pending and were not claimed. Cycle 24 supersedes this record; no current test or fixture-count claim is carried forward. |
| post-cycle-23 independent Sol-high review cycle 24 (2026-09 follow-up) | Superseded | Historical scope: cover all four ECMAScript line terminators (`LF`, `CR`, `U+2028`, and `U+2029`) in comment masking, and preserve statement-boundary semantics when a block-close prefix is followed by an update. Its verification remains historical exactly: `npm run validate` passed and the main validator printed 12 skill Markdown files; direct `node dev/validate-fixtures.mjs` passed its 2 cases (the source header declared 273 sequential controls); and separate `node --check` checks passed for the validator and fixture runner. CI and active Codex-install parity remained pending and were not claimed. Cycle 25 supersedes this record; no current test or fixture-count claim is carried forward. |
| post-cycle-24 independent Sol-high review cycle 25 (2026-09 follow-up) | Superseded | Scope: add causal same-line block controls; distinguish Unicode line terminators inside block comments from real statement boundaries; require a class declaration's own closing brace before accepting its update path; and exclude private keyword-method lookalikes from public method matching. The cycle-24 273-control result remains historical evidence and is not silently rewritten. Verification completed: `npm run validate` passed and the main validator printed 12 skill Markdown files; direct `node dev/validate-fixtures.mjs` passed its 2 cases (the source header declares 277 sequential controls); and separate `node --check` checks passed for the validator and fixture runner. CI and active Codex-install parity remain pending and are not claimed. Cycle 26 supersedes this closure record; no current test, fixture-count, CI, or active-install parity claim is carried forward. |
| post-cycle-25 independent Sol-high review cycle 26 (2026-09 follow-up) | Superseded | Scope: cover `export default` class declaration modifiers and preserve statement-boundary update parsing for Unicode and escaped class identifiers. The cycle-25 277-control result remains historical evidence and is not silently rewritten. Verification completed: `npm run validate` passed and the main validator printed 12 skill Markdown files; direct `node dev/validate-fixtures.mjs` passed its 2 cases (the source header declares 289 controls); and separate `node --check` checks passed for the validator and fixture runner. CI and active Codex-install parity remain pending and are not claimed. Cycle 27 supersedes this closure record. |
| post-cycle-26 independent Sol-high review cycle 27 (2026-09 follow-up) | Superseded | Historical scope: constrain static workflow checks to literal, unescaped `MODULES`/`AUDIT_UNITS` references and literal property/index and mutator names; escaped spellings are a runtime-only boundary, including escaped `IdentifierName` forms in dotted access (`\uXXXX`/`\u{...}`) and escape sequences inside quoted bracket keys, with runtime `deepFreeze` retained as the fail-fast backstop; and add coverage for short braced Unicode class identifiers, `case`/`default`/postfix class boundaries, and correct identifier boundaries. Cycle 26's 289-control verification and cycle 27's 313-control evidence remain historical and are not silently rewritten. Current tests, fixture count, CI, and active Codex-install parity are pending and are not claimed. |
| post-cycle-27 independent Sol-high review cycle 28 (2026-09 follow-up) | Superseded | Historical scope: preserve the runtime-only escaped-spelling boundary for dotted `IdentifierName` access and escape sequences inside quoted bracket keys; add causal controls for valid braced escapes; cover nested-brace switch labels; and preserve the astral postfix identifier boundary. Cycle 27's 313-control evidence remains historical and is not silently rewritten. Its verification remains historical exactly: `npm run validate` passed (the main validator printed 12 skill Markdown files); direct `node dev/validate-fixtures.mjs` passed its 2 cases (the source header declares 325 controls); and separate `node --check` passed for the validator and fixture runner. Cycle 29 supersedes this closure record; current control count, tests, CI, and active Codex-install parity are pending and are not claimed. |
| post-cycle-28 independent Sol-high review cycle 29 (2026-09 follow-up) | Superseded | Scope: cover `case`/`default` recognition across prior-clause automatic-semicolon-insertion boundaries; add causal ternary and multi-clause controls that prove the parser reaches the relevant mutation path; and keep the control ledger physically sequential so numbering cannot silently drift. Cycle 28's 325-control evidence remains historical and is not silently rewritten. Verification completed: `npm run validate` passed (the main validator printed 12 skill Markdown files); direct `node dev/validate-fixtures.mjs` passed its 2 cases (the source header declares 339 sequential controls); and separate `node --check` passed for the validator and fixture runner. CI and active Codex-install parity remain pending and are not claimed. Cycle 30 supersedes this closure record; its current tests, fixture count, CI, and active Codex-install parity are pending and are not claimed. |
| post-cycle-29 independent Sol-high review cycle 30 (2026-09 follow-up) | Integrated | Scope: distinguish a decimal/consequent ternary expression from optional chaining at the parser boundary, and add causal controls proving that each supported and rejected spelling reaches the relevant mutation path. Cycle 29's 339-control evidence remains historical and is not silently rewritten. Verification completed: `npm run validate` passed (the main validator printed 12 skill Markdown files); direct `node dev/validate-fixtures.mjs` passed its 2 cases (the source header declares 343 sequential controls); and separate `node --check` passed for the validator and fixture runner. CI and active Codex-install parity remain pending and are not claimed. |

## Historical-count erratum

The crypto review's prose count was inconsistent with its listed candidates. The integrated change set contains the five in-scope crypto findings plus two explicitly out-of-scope candidates; it is not a thirteen-item result. Use the rule ledger and changelog bullets as authoritative, not the old arithmetic sentence.

## Review quality gate

Every future gap review must include: (1) a candidate inventory, (2) positive and negative calibration examples, (3) a primary source per accepted rule, (4) an explicit out-of-scope list, and (5) a post-merge regression record in a **committed** artifact — the release's `CHANGELOG.md` entry, plus a row in the table above whenever the review corrects an earlier one. (`docs/checkpoints/` is deliberately untracked local session state and cannot carry a repo-level obligation.)
