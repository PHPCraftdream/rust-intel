# Checkpoint — 2026-08-14 14:47

## Session summary

Работа над `rust-intel` (`D:\dev\rust\rust-intel`) — Claude Code / Codex skill, каталогизирующий категории Rust-багов, которые проходят `rustc`/`clippy`/`cargo test`, но ломаются в проде. 58 нумерованных категорий, 6 тиров (A–F), тема-модули под `skill/`, ядро/индекс — `skill/SKILL.md`. Второй канал дистрибуции — Codex (`.codex-plugin/`, `bin/install-codex.js`, зеркало `skills/rust-intel/`, генерируемое `dev/sync-mirror.mjs` и обязанное быть байт-в-байт идентичным `skill/`).

Сессия началась с продолжения (после auto-compact) работы над v0.5.0: серия из трёх раундов независимого код-ревью (`docs/reviews/commit-458e821-review.md`, `commit-0a31099-review.md`, `commit-c9fb62a-review.md`) над правками из предыдущей части сессии (Codex-дистрибуция, review-of-reviews по v0.4.7, first CI). Каждый раунд ловил реальные регрессии, включая дважды повторившийся класс бага: shell-инсталлятор (`rust-cc-install.sh`) содержал path-traversal/symlink bypass в overlap-guard, невидимый на машине пользователя (Windows/MSYS лексически нормализует `..` раньше, чем `-e`/`-d`, маскируя баг, который реален на настоящем POSIX/Ubuntu-CI). Баг подтверждался вручную — либо трассировкой обхода вручную, либо переписыванием того же алгоритма на JS против смоделированной POSIX-ФС — прежде чем чиниться. Финальный фикс — `canonical_candidate()` в `rust-cc-install.sh` итерирует walk→resolve→normalize до неподвижной точки, а не один раз.

После закрытия всех находок v0.5.0 был опубликован: тег `v0.5.0` → GitHub Actions `npm-publish` (npm `rust-intel-cc@0.5.0`, provenance) → GitHub Release → установка через `npx` для Claude (`~/.claude/skills/rust-intel/`) и Codex (`~/.agents/skills/rust-intel/`).

Далее пользователь попросил изучить внешний блог (`hotpath.rs/blog/profiling-rust-guide`) на предмет заимствований. Из 6 тезисов статьи 4 уже были покрыты дословно совпадающими примерами в спеке; 2 пробела — N+1-запросы (не Rust-специфично, оставлено в roadmap) и ненадёжность wall-clock как перф-оракула в CI. Второе было принято к реализации, но переосновано не на вендорском блоге, а на first-party документации `criterion` (FAQ прямо предостерегает против wall-clock-гейтов на GitHub Actions) — что обнажило внутреннее противоречие в существующем §E6. Реализовано: переписан §E6 «Lock the win» (гейт — детерминированный счётчик, не wall-clock), новый BANNED-буллет в §D1 (порог по elapsed-времени в тесте), две триггер-строки в `SKILL.md`, запись в `references/sources.md`. Собственное само-ревью до реализации поймало и исправило ложное направление в черновике («debug-порог краснеет под `--release`» — на самом деле release быстрее, значит становится вакуумным, не красным). Это стало релизом **v0.5.1** — потребовался явный вопрос пользователю про номер версии (v0.5.0 уже был опубликован), после подтверждения: бамп → коммит → тег → пуш → CI зелёный → npm-publish зелёный → GitHub Release → переустановка для обеих платформ.

Последняя крупная задача: пользователь попросил изучить методологию соседнего Rust-проекта (`D:\dev_forge\ms-project-runtime-r2`) — 24 атомарных правила в `.claude/rules/`, `AGENTS.md`, агент `logic-defect-prover` с вердиктами `proven`/`disproven`/`unreachable`/`unprovable`. Решено НЕ брать архитектурные правила (DDD/гексагон-специфика одного кодбейса, вне scope rust-intel — собственный гейт спеки исключает правила, где LLM не ошибаются чаще людей), но взять одну идею: ось доказанности находки в выводе аудита. Реализовано как `evidence: pattern|traced|proven` + `reachedFrom` в `FINDINGS_SCHEMA` воркфлоу и параллельно в `commands/rust-intel-cc/audit.md` (серийный путь), плюс секция `unreachable` (совпадения без пути вызова — не находка, но не выбрасывается молча). Запущен независимый ревью-агент (`@ox`, subagent_type `ox`, ~90K токенов) на эту правку — вернул 11 находок + 5 minor, все верифицированы вручную (несколько — прямым воспроизведением: например Finding 8 показал, что валидатор реально пропускал регресс `required`-полей несмотря на заявление в CHANGELOG о защите). Все находки исправлены в коммите `d31c91e`, включая более широкое решение, чем предлагал ревьюер (полностью убрал evidence из ключа сортировки находок, а не просто задал приоритет между tier и evidence — потому что категории без графа вызовов вроде §A1/Tier D/§F2 по конструкции всегда `pattern`, и сортировка по evidence несправедливо топила полностью установленные находки).

**Текущее состояние: чистая точка.** Коммит `d31c91e` — последний в `main`, НЕ запушен, версию не бампал (манифесты всё ещё на 0.5.1 — это правка audit-схемы, не спеки, PATCH-масштаб для контента, но с явно задокументированным в CHANGELOG "compatibility note" про два consumer-visible слома схемы). TaskList пуст. Не было активных `/goal`/`/babysit`/`/loop`.

## Active goal

none — не устанавливался в этой сессии.

## TaskList

Пусто (все задачи предыдущих раундов правок закрыты и вычищены).

## Decisions

- **Не тегировать/не публиковать evidence-axis правку без явной команды** — версия осталась 0.5.1 в манифестах, коммит `d31c91e` создан, но не запушен. Ждём явного «тег/пуш/публикация» как во всех предыдущих релизах этой сессии.
- **Убрать evidence из ключа сортировки находок полностью**, а не просто разрешить конфликт tier-vs-evidence приоритетом (как предлагал ревьюер) — потому что категории без применимого графа вызовов (манифесты §A1, semver-форма §B1*/§C1*, весь Tier D, §F2) по конструкции всегда `pattern`, и сортировка по evidence конфликтует с их собственным заголовком поля («HOW established, never how confident»).
- **Не брать архитектурные правила ms-project-runtime-r2** (DDD/гексагон/no-&mut-params/no-flag-args) — они не проходят собственный acceptance-гейт спеки («LLMs don't err more than humans ⇒ that's just style»); взята только методологическая ось доказанности.
- **Основание правки §E6/§D1 — first-party документация `criterion`, не вендорский блог `hotpath.rs`**, который инициировал исследование, но чьи цифры признаны непригодными как источник (self-produced демо-цифры).
- **Валидатор должен пиннить целые `required: [...]`-литералы, а не только имена свойств** — обнаружено воспроизведением реального регресса (Finding 8 внешнего ревью): удаление поля из `required` проходило `dev/validate.mjs` зелёным, хотя CHANGELOG утверждал обратное.

## Open questions

- Пушить/тегировать/публиковать ли коммит `d31c91e` (evidence-axis правка) как часть релиза, и если да — под каким номером версии (сейчас манифесты на 0.5.1, но контент этого коммита ещё не выпускался под этим тегом — нужно решить: включить в новый тег 0.5.1 если он ещё не запушен как релиз с тегом, или бампнуть до 0.5.2). Пользователь ещё не дал команды на публикацию этой конкретной правки.

## Repo state

```
?? .githooks/
?? docs/checkpoints/
```
```
d31c91e rust-intel: evidence axis for audit findings (pattern/traced/proven)
76050f5 rust-intel 0.5.1: performance CI gates measure the wrong thing
aed13ff ci: move to Node 24 and actions/{checkout,setup-node}@v7
fe3f4c4 rust-intel 0.5.0: close the composed installer overlap bypass
c9fb62a rust-intel 0.5.0: address independent review of 0a31099 (4 findings)
```

Примечание: `d31c91e` создан ЛОКАЛЬНО поверх уже запушенного и опубликованного `76050f5` (тег `v0.5.1` существует на GitHub, npm `rust-intel-cc@0.5.1` опубликован) — то есть `main` в этой рабочей копии на 1 коммит впереди `origin/main` и впереди опубликованного релиза.
